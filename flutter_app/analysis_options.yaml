# Static analysis configuration for Trans-Hub (TH-003).
#
# Strict linting + analyzer settings per the modernization plan:
#   - strict-casts / strict-raw-types / strict-inference
#   - treat selected lints as errors to gate CI
#   - unused code detection
#
# Run with: `flutter analyze` (CI gates on this).
include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    # Promote correctness lints to errors so CI fails fast.
    invalid_annotation_target: ignore
    todo: ignore
    unused_import: error
    unused_local_variable: error
    unused_element: error
    dead_code: error
    avoid_print: warning
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "build/**"

linter:
  rules:
    # Style & safety
    always_declare_return_types: true
    annotate_overrides: true
    avoid_empty_else: true
    avoid_init_to_null: true
    avoid_relative_lib_imports: true
    avoid_returning_null_for_future: true
    avoid_unused_constructor_parameters: true
    await_only_futures: true
    camel_case_types: true
    cancel_subscriptions: true
    close_sinks: true
    directives_ordering: true
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_single_quotes: true
    require_trailing_commas: true
    sort_child_properties_last: true
    unawaited_futures: true
    unnecessary_brace_in_string_interps: true
    unnecessary_null_checks: true
    use_key_in_widget_constructors: true
